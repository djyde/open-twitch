.grid
  .form
    %h2.center 个人信息
    = simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
      = f.error_notification
      = f.input :description
      = f.input :current_password, hint: "输入当前密码以确认身份", required: true
      = f.button :submit, "更新" , {style: 'margin-top: 24px;'}